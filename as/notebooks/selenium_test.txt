
  import os
  from pathlib import Path

  print("ROOT_DIR:", ROOT_DIR)
  print(".env exists:", (ROOT_DIR / ".env").exists())
  print(".env path:", ROOT_DIR / ".env")

  # 讀 .env 內容看是否真的有 SNOW_BASE_URL
  env_file = ROOT_DIR / ".env"
  if env_file.exists():
      for line in env_file.read_text().splitlines():
          if "SNOW" in line.upper():
              print(" ", line)
  else:
      print("  .env FILE NOT FOUND at that path!")

  print("\nos.getenv SNOW_BASE_URL:", repr(os.getenv("SNOW_BASE_URL")))